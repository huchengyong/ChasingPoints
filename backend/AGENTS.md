# BACKEND KNOWLEDGE BASE

## OVERVIEW
`backend/` 是 go-zero REST API 服务主目录。它不仅负责常规 HTTP 接口，还负责对局级和用户级 WebSocket、Redis 短信验证码、UniPush 推送、球房地理编码任务以及 Gorm 数据访问。

## STRUCTURE
```text
backend/
├── chasing_points.api        # 后端契约单一真源
├── chasing_points.go         # 服务入口，注册 HTTP + WebSocket 路由
├── go.mod                    # go-zero + Gorm + Redis + Ali SMS + UniPush 依赖
├── etc/                      # API 配置，支持 .env 注入
├── internal/                 # handler / logic / model / svc / pkg / utils / types
├── migrations/               # goose SQL 迁移
├── goose.sh                  # 基于 .env 的 migration 脚本
└── readme.md                 # 当前基本为空，不要依赖它获取事实
```

## DOMAIN MAP
- `chasing_points.api` 当前覆盖 `auth`、`public`、`user`、`match`、`opponent`、`rank`、`achievement`、`friend`、`follow`、`social`、`challenge`、`tournament`、`season`、`venue`、`rules`、`notification`、`share`、`stats`，以及 admin 端相关接口。
- `chasing_points.go` 额外注册了两个 WebSocket 路由：`/api/match/ws`、`/api/user/ws`。
- `internal/svc/service_context.go` 集中初始化 MySQL、Redis、阿里云短信、UniPush、地理编码 worker，以及所有 model。
- `internal/logic/<group>/` 是接口入口 logic 的标准目录，当前已覆盖 `achievement`、`admin`、`auth`、`challenge`、`eventnews`、`follow`、`friend`、`match`、`notification`、`opponent`、`public`、`rank`、`rules`、`season`、`share`、`social`、`stats`、`tournament`、`user`、`venue`。
- 根目录 `internal/logic/*.go` 只保留跨领域共享 helper / service / protocol / payload / summary / settlement 等公共层文件。

## WHERE TO LOOK
| Task | Location | Notes |
|------|----------|-------|
| 新增/修改接口契约 | `chasing_points.api` | 改完立刻跑 goctl |
| 启动配置 | `etc/chasing_points-api.yaml` | 结合 `.env` 注入 |
| 服务入口与 WS 路由 | `chasing_points.go` | 不要漏看手工注册的 WS 路由 |
| 迁移脚本 | `migrations/*.sql` | 迁移和 model 必须同步 |
| 迁移执行 | `goose.sh` | 自动读取 `.env` 并包装 goose |
| 依赖注入 | `internal/svc/service_context.go` | model、Redis、短信、Push 全在这里 |
| 接口入口逻辑 | `internal/logic/<group>/*.go` | 与 `internal/handler/<group>/*.go` 一一对应 |
| 公共层 helper | `internal/logic/*.go` | 只放跨领域共享工具，不放接口入口 logic |
| 管理后台逻辑 | `internal/logic/admin/*.go` | admin 登录、首页、用户、对局、球馆审核、赛事情报 |
| 赛事情报领域 | `internal/model/event_news.go`, `internal/model/event_news_stage.go`, `internal/logic/eventnews/*.go` | 当前使用 `event_news_events + event_news_stages` 双表结构，由 migration 管理 |

## CONVENTIONS
- 修改 `.api` 后必须执行：`goctl api go --api chasing_points.api --dir . --style go_zero --home ~/.goctl/default`。
- `.api` 中 `service` 名固定为 `chasing_points-api`。
- 不要编辑任何 `Code generated by goctl. DO NOT EDIT.` 文件。
- `internal/types/types.go`、`internal/handler/routes.go` 属生成产物，禁止手改。
- 新增接口 logic 时，优先落在对应的 `internal/logic/<group>/`；根目录 `internal/logic` 不再新增 handler 一一对应的 `*_logic.go`。
- 重新跑 goctl 后，如果生成了新的 `internal/logic/<group>/` `todo` 空壳文件，只有在同步补齐真实逻辑、handler import 和测试后才允许提交。
- 逻辑层不直接操作数据库，统一经 `internal/model`。
- 需要用户 ID 时必须调用 `internal/utils/jwt_parser.go` 的 `GetUserIDFromCtx`。
- 迁移 SQL 必须包含 `+goose Up` / `+goose Down`，建表语句带 `IF NOT EXISTS`。
- MySQL 迁移里不要默认使用 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 或 `DROP COLUMN IF EXISTS`；当前环境可能不支持这类语法。给已有表补字段时，优先通过 `information_schema.COLUMNS` 判断后再动态执行 `ALTER TABLE`，保证老版本 MySQL 也能跑。
- 生产代码里的 model 构造函数不要调用 `AutoMigrate`；线上 schema 只认 `backend/migrations/*.sql`。如果 sqlite 单测需要表结构，必须在测试里显式准备 schema，不要让 `NewXxxModel` 顺手建表。
- 修改业务表结构后，要同步补齐 Gorm 模型、json 标签和必要的 model 方法。
- 新增测试如果依赖 migration 管理的表，必须显式建 schema；不要默认指望 model 构造函数帮你建表。

## ANTI-PATTERNS
- 不要把 `.api` 当“参考文档”，它是实际契约源；改接口先改它。
- 不要忽略 `chasing_points.go` 里手工注册的 WebSocket 路由。
- 不要在 logic 里新建 MySQL/Redis/SMS/Push 客户端；统一从 `ServiceContext` 注入。
- 不要把新的接口入口 logic 再写回根目录 `internal/logic/*.go`。
- 不要假设后端只有 HTTP 接口；对局和用户通知还依赖 WebSocket 实时链路。
- 不要把 `readme.md` 当成当前后端说明文档；现在几乎没有内容。

## QUICK COMMANDS
```bash
# 运行服务
go run chasing_points.go -f etc/chasing_points-api.yaml

# 生成 API 模板
goctl api go --api chasing_points.api --dir . --style go_zero --home ~/.goctl/default

# 数据库迁移
./goose.sh status
./goose.sh up
./goose.sh create <name>

# 后端测试
go test ./...
```
