## 1. 数据模型与 API 契约

- [x] 1.1 新增兼容 MySQL 的 goose 迁移，通过 `information_schema.COLUMNS` 安全添加 `match_format` 和 `target_wins`，默认保持 `legacy/0` 且不回填存量中八、美九对局
- [x] 1.2 更新 Gorm `Match` 模型，新增逐局赛制常量、1～65目标范围、129局上限及仅适用于 `game_type=3/4` 的规范化 helper
- [x] 1.3 补充 migration 与 model 测试，覆盖 legacy、free、race_to、目标1/65边界、目标66拒绝和其他球种不启用
- [x] 1.4 更新 `backend/chasing_points.api`，为创建、当前对局、详情、公开详情、同步快照和 WebSocket 增加 `match_format`、`target_wins`、`can_change_match_format`，并新增通用赛制更新接口
- [x] 1.5 修改 `.api` 后立即运行 goctl 生成代码，检查生成的 types、routes、handler 与 logic 入口没有未接入的 `todo` 空壳

## 2. 后端创建、赛制更新与快照

- [x] 2.1 更新创建校验：新版中八、美九显式 `free/0` 或合法 `race_to/1～65`，旧客户端缺省字段继续创建 `legacy`，斯诺克和九球追分保持原行为
- [x] 2.2 实现 `UpdateMatchFormat` logic/handler：事务行锁、创建者权限、仅 free/race_to、无动作/无局/无裁判/无结束确认、base revision 校验及 legacy 不可转换
- [x] 2.3 统一计算 `can_change_match_format`，并在当前对局、详情、公开详情、写响应和双方视角同步快照中返回规范化赛制状态
- [x] 2.4 在 WebSocket `sync`、`round_end`、兼容比分消息和结束消息中携带赛制字段，赛制更新成功后广播同一已提交 revision 的权威快照

## 3. 后端局胜、结束条件与防绕过

- [x] 3.1 为中八、美九 free/race_to 对局实现事务化 `EndRound` 分支，校验 winner、`score=1`、幂等键和 revision，并原子写入局记录、胜方 `+1` 与动作日志
- [x] 3.2 在同一 `EndRound` 事务内判断自由局数第129局和抢 N 达标，确保并发请求不能写出第130局、超过目标胜局或重复触发结束
- [x] 3.3 复用正常结束协调：练习赛达到条件后直接结算，排位赛冻结比分并进入双方确认，绑定裁判时沿用现有裁判完成语义
- [x] 3.4 更新 `FinishMatch`、结束请求和 viewer capabilities：free 至少一局后可按实际比分正常结束并允许平局；race_to 未达目标拒绝正常提前结束
- [x] 3.5 达到目标或129局上限、以及排位待确认期间，拒绝后续结束一局和其他记分写入；争议恢复后允许撤销纠错但不得继续越过结束边界
- [x] 3.6 对 free/race_to 的中八、美九拒绝 `MatchScore` 与 `MatchFoul` 直接修改整场胜局，同时保持 legacy、斯诺克和九球追分既有入口不变
- [x] 3.7 核对撤销、幂等重放、结束后处理、成就/段位/信誉、通知与缓存失效链路，确保自动结束和确认结束只执行一次副作用

## 4. 移动端创建与赛制交互

- [x] 4.1 更新 `app/api/match.js` 与开局 payload：新版中八、美九默认显式提交 `free/0`，新增赛制更新门面，九球追分和旧字段缺失保持兼容
- [x] 4.2 新增或提炼逐局赛制纯函数，覆盖 free/race_to 规范化、1～65选择项、`2N-1` 提示、129局上限和自由局数平局文案
- [x] 4.3 扩展进行中页面赛制区到中八和美九，创建者仅在 `can_change_match_format=true` 时可通过滚动选择器修改，首局后保留只读文案
- [x] 4.4 接入 HTTP 与 WebSocket 权威快照，赛制保存冲突、首局结束、到点结束和断线重连均按最新 revision 收敛，不由客户端比分自行判定结束
- [x] 4.5 自由局数至少完成一局后提供正常结束入口并允许平局；抢 N 未达目标时隐藏正常结束入口，达到目标或待确认后冻结记分操作
- [x] 4.6 保持球种 Modal、邀约球种、斯诺克专属规则和九球追分 UI 不变；重赛作为新对局按新版默认自由局数创建，不继承旧赛制

## 5. 自动化验证与回归

- [x] 5.1 增加后端创建与更新测试，覆盖中八/美九 free、抢1/10/65、非法格式、目标66、legacy、对手/裁判/已有动作或局拒绝及真实并发 revision 竞争
- [x] 5.2 增加后端结算测试，覆盖自由局数领先/落后/平局结束、零局拒绝、第129局自动结束、第130局拒绝、抢 N 的 N-1 与 N 边界
- [x] 5.3 增加练习直结、排位确认/争议、裁判完成、幂等重放和并发结束一局测试，确认比分、状态、revision 与结算副作用只提交一次
- [x] 5.4 增加防绕过与兼容测试，确认灵活赛制拒绝 `score!=1`、通用加分和犯规，legacy、斯诺克及九球追分行为不回归
- [x] 5.5 增加当前对局、详情、公开详情、同步快照和 WebSocket 一致性测试，覆盖双方不同的 `can_change_match_format` 权限视图
- [x] 5.6 增加 App 纯函数、开局 payload、类型3/4赛制编辑器真实 SFC 交互和类型2不展示测试，覆盖65胜与129局文案及锁定状态
- [x] 5.7 对所有修改过的 Go 文件运行 gofmt，在 `app/` 运行 `node --test tests/*.test.mjs`，在 `backend/` 运行 `go test ./...` 并修复本变更引入的回归
- [x] 5.8 人工核对普通/排位、创建者/对手/裁判、中八/美九、自由局数平局、抢65的129局上限、旧客户端和断线重连关键路径
