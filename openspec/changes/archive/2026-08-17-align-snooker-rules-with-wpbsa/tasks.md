## 1. 数据结构与 API 契约

- [x] 1.1 新增兼容 MySQL 版本的 migration，为 `matches` 增加 `snooker_rules_version`、`best_of_frames`、`starting_actor` 字段，保持现有记录为版本1并为新版本提供必要索引/默认值
- [x] 1.2 同步更新 `backend/internal/model/match.go` 的 Gorm 字段、创建逻辑和读模型，并补 migration/model 一致性测试
- [x] 1.3 修改 `backend/chasing_points.api`：扩展斯诺克创建与快照字段，新增 `/api/match/snooker/stroke` 和 `/api/match/snooker/frame-action` 的请求响应契约
- [x] 1.4 修改 `.api` 后立即运行 `goctl api go --api chasing_points.api --dir . --style go_zero --home ~/.goctl/default`，检查生成差异并确认没有遗留新的 `todo` 空壳引用
- [x] 1.5 在 `app/api/match.js` 增加斯诺克击球和局动作门面，并更新开始对局参数注释，页面不得直接调用请求工具

## 2. 版本2事件与状态机基础

- [x] 2.1 定义版本2斯诺克事件 payload、球枚举、phase、ball-on、犯规决定和局结束原因，并实现严格的 `ExtraData` JSON 编解码及未知版本拒绝逻辑
- [x] 2.2 扩展 `SnookerRoundState`，加入击球方、开球方、visit、当前单杆、红球剩余、自由球、D区手中球、Miss 警告、重置黑球和局结果字段
- [x] 2.3 实现版本2当前局初始化和逐局开球方轮换，验证首局 actor 与正奇数 `best_of_frames`
- [x] 2.4 实现红球、自选彩球、最后一红和彩球顺序清台的纯状态转移，分值全部由球和入袋数量推导
- [x] 2.5 实现合法未进球转移，正确结束 visit、切换击球方，并覆盖最后一红后未进球直接进入黄球的场景
- [x] 2.6 为初始化、红彩交替、多红同杆、最后一红、顺序清彩、非法球值和非法 actor 增加表驱动单元测试

## 3. 犯规、自由球与 Foul and a Miss

- [x] 3.1 实现4至7分犯规校验、犯规杆零入袋得分、真实离台红球扣减和三种犯规后继续方式
- [x] 3.2 实现裁判宣告自由球、指定球合法性、自由球复位、真实红球扣减以及清彩目标是否推进的状态转移
- [x] 3.3 实现 Foul and a Miss、原位重打序列、第二次失败警告、警告后再次失败判局及序列清零规则
- [x] 3.4 增加犯规非法罚分、犯规红球离台、自由球作红球、自由球与真实红球同杆、清彩自由球及连续 Miss 的表驱动测试

## 4. 重置黑球、认输与赛制结算

- [x] 4.1 实现最后黑球首次入袋或犯规后的比分判断：非平分直接结束局，平分进入 `respotted_black_pending`
- [x] 4.2 实现重置黑球先打方选择、D区手中球状态，以及第一次合法黑球得分或犯规立即结束局
- [x] 4.3 实现基于当前 phase、ball-on、红球剩余和自由球状态的台面剩余可得分计算
- [x] 4.4 实现本局认输提议、条件校验、接受/拒绝及裁判记录已接受认输，禁止无需罚分时提前认输
- [x] 4.5 实现整场认输/裁判判负、目标胜局自动完成比赛，以及未达到目标胜局时才允许开始下一局
- [x] 4.6 增加最后黑球平分、重置黑球得分/犯规、过早认输、有效认输、开球轮换和达到目标胜局的测试

## 5. 后端写入、回放与同步

- [x] 5.1 实现版本2事件解释器，从当前局有效动作确定性重放比分、状态、visit 和局结束结果，并保留版本1现有构建逻辑
- [x] 5.2 补齐生成的斯诺克 stroke logic/handler：校验权限、规则版本、幂等和 revision，在单事务中写入动作、更新当前局比分并按需生成局记录
- [x] 5.3 补齐生成的 frame-action logic/handler：处理重置黑球、认输和裁判判局，并复用同一 revision/快照响应链路
- [x] 5.4 在版本2斯诺克对局中拒绝通用 `matchScore`、`matchFoul`、客户端指定胜方的 `endRound` 和普通手工结束比赛，同时保持版本1及其他球种行为不变
- [x] 5.5 扩展 HTTP 写响应、对局详情、当前对局、公开观战和 WebSocket 快照，使其统一返回规则版本、phase、ball-on、striker、visit、红球剩余、自由球、Miss 和重置黑球状态
- [x] 5.6 扩展 `startNextRound`，只允许已合法结束且整场未完成的版本2斯诺克对局开新局，并按赛制设置下一局开球方
- [x] 5.7 增加 stroke/frame-action 的权限、幂等、revision 冲突、双方视角和 HTTP/WebSocket 快照一致性测试

## 6. 撤销、单杆与奖励结算

- [x] 6.1 修改版本2撤销逻辑：标记最后动作撤销后重放当前局，不再通过分值反向猜测自由球、红球、visit 或 Miss 状态
- [x] 6.2 支持撤销结束当前局的最近动作，删除对应 Round、回退整场局比分并恢复前一局状态；继续禁止撤销已完成整场比赛
- [x] 6.3 将版本2最高单杆、50+、破百和147计算改为按 round、actor、visit 聚合合法得分，排除罚分并允许展示自由球产生的高于147单杆
- [x] 6.4 修改版本2成就与排位奖励回放，遇到非法或无法解码事件时阻止结算，版本1已完成对局继续走旧统计
- [x] 6.5 增加跨合法未进球、对手犯规、新 visit、精确147、自由球155、撤销和异常事件阻断结算的测试

## 7. 移动端版本2斯诺克流程

- [x] 7.1 扩展开始对局界面和纯逻辑校验，为标准斯诺克选择正奇数总局数和首局开球方，并保留其他球种原流程
- [x] 7.2 新增移动端斯诺克状态纯函数，根据快照生成当前击球方、目标球、合法入袋选项、未进球操作和阶段提示
- [x] 7.3 修改 `app/subPages/match/playing.vue`，按 `snooker_rules_version` 分流版本1旧面板和版本2权威状态面板，版本2不得任意选择非当前击球方得分
- [x] 7.4 为版本2实现多红同杆、彩球/自由球选择和“合法未进球”提交，并准确显示当前 visit 与单杆
- [x] 7.5 实现犯规操作面板：4至7分、离台红球数、自由球判定、Foul and a Miss、从现状继续/犯规方继续/原位重打选择
- [x] 7.6 实现重置黑球先打方选择、本局认输提议/接受/拒绝和整场认输交互，移除版本2按领先比分自动判局行为
- [x] 7.7 扩展 HTTP/WS 快照应用和裁判/选手反馈文案，确保固定选手身份、当前击球方和犯规受益方一致
- [x] 7.8 更新 `playing.scss` 的必要样式，保持 UniApp 跨端、小屏和 WXSS 选择器约束，不重构无关页面区域

## 8. 规则文案与数据更新

- [x] 8.1 修正 `backend/internal/model/rules_seed.go` 中斯诺克开球、同时首碰、斯诺克定义、自由球和147/155说明
- [x] 8.2 新增 migration 按 `(category, content_type, title)` 更新数据库已有错误规则行，并提供可逆 Down 内容
- [x] 8.3 增加规则 seed 和 migration 文件测试，确认新库与已有库都会得到一致的官方规则文案

## 9. 回归测试与验收

- [x] 9.1 在 `app/tests` 增加版本2状态纯函数和页面结构测试，覆盖红彩、未进球、犯规决定、自由球、Miss、重置黑球、认输、版本1兼容和裁判双方视角
- [x] 9.2 在 `app/` 运行 `node --test tests/*.test.mjs` 并修复本变更引入的失败
- [x] 9.3 在 `backend/` 运行斯诺克、对局写入、撤销、快照、奖励和 migration 相关定向测试
- [x] 9.4 在 `backend/` 运行 `go test ./...`，确认其他球种、排位、成就和 WebSocket 链路无回归
- [x] 9.5 按 WPBSA 关键场景手工验收：开局非法黑球、最后一红未进、犯规红球离台、自由球、连续 Miss、最后黑球平分、重置黑球、过早认输和合法147

## 10. Review 修复与发布加固

- [x] 10.1 为创建请求增加显式 `snooker_rules_version` 协商：旧客户端缺省创建版本1，新移动端所有斯诺克入口显式请求版本2，并更新 API 后立即运行 goctl
- [x] 10.2 统一移动端所有斯诺克创建入口的总局数和首局开球方选择，移除首页、排行榜和用户页静默固定三局/发起方开球
- [x] 10.3 将斯诺克幂等编号绑定规范化动作内容和 base revision；并发重放不得重复广播，已自动完赛动作重试需补偿可幂等的完赛后处理
- [x] 10.4 收紧版本2事件字段组合校验，并在当前局混入旧版或未知动作时让确定性重放失败
- [x] 10.5 替换超过微信 `showActionSheet` 六项限制的多红选择，确保普通多红和自由球加红球在各端可操作
- [x] 10.6 补齐自由球未入袋、仅真实目标球入袋、两者同时入袋和合法未进球流程，并为自选彩球未进球记录实际目标球
- [x] 10.7 认输待确认期间禁用击球操作，修正重置黑球及无当前击球方时的选手身份文案
- [x] 10.8 增加旧客户端协商、幂等 payload、混合动作重放、跨端计数、自由球和认输 UI 测试，并运行 app/backend 全量测试、race、SFC 编译与 OpenSpec 严格校验
