## 1. 核实官方数据与现有边界

- [x] 1.1 读取后端约定及现有采集、图片镜像测试，确认 CLI、自动任务、存储和发布的共同调用链；记录需要保持的兼容行为。
- [x] 1.2 用只读请求核实官方赛季目录、赛事、对阵分页和排序契约以及 2023 年可用数据，保存脱敏样本与观测日期；确认比分/null、分配标记、状态、待定选手和轮空/退赛字段语义，确定分页结束校验、页数保护及重试等待预算，不把未知覆盖记录为已完成。

## 2. 补采入口与完整扫描

- [x] 2.1 在现有参数解析中加入 `--backfill`、UTC 默认范围、可独立覆盖的端点、互斥及仅限斯诺克校验；补充默认日期、跨年边界、非法参数及原有模式兼容测试。
- [x] 2.2 补齐赛事列表分页及赛季目录完整性校验，复用一次全局对阵扫描；在目标过滤前检查原始页 ID 进展，覆盖第二页、短页、空页、无关赛事页、重复页、矛盾 ID、部分未知赛季名称、200 错误对象及缺失 data。
- [x] 2.3 在现有 JSON 请求重试中支持 429/5xx 和有界退避，覆盖 `Retry-After`、最多 3 次尝试、永久错误和取消；避免真实长等待的测试。
- [x] 2.4 使用 `signal.NotifyContext` 将 CLI 中断连至 HTTP、退避、预检及数据库事务，验证取消能结束工作且输出已知阶段；保留固定页数保护。

## 3. 历史保留与存储一致性

- [x] 3.1 将显式补采语义传入共享 service，只在补采中跳过缺失对阵清理，验证自动同步仍保持原有行为和游标规则。
- [x] 3.2 在 dry-run/上传之前共用批量只读预检：检查 ID、关联、日期、软删除和既有重复 ID；命中软删除时明确失败，覆盖预览与正式导入结果一致及无需自动恢复。
- [x] 3.3 为选中对阵保留必要字段存在性，跳过并报告缺失比分/分配信息/不明状态而保留旧值；测试明确 0 分、特殊状态和不能仅按过去时间推导完赛，并保持非补采行为兼容。
- [x] 3.4 在现有测试中验证重复补采四类记录不重复增长、业务 ID 稳定、缺失旧对阵的保留与报告、合并后赛事状态/摘要一致、事务失败回滚，以及范围外赛事/对阵/赛讯和人工记录不变；允许引用的共享官方球员更新。
- [x] 3.5 验证 dry-run 无数据库/缓存/七牛写入，`--publish=false` 命中已发布赛讯会下架且预览显示数量；保留现有图片镜像规则。

## 4. 结果核对与操作说明

- [x] 4.1 扩展摘要，区分源站返回、有效准备、数据库保留和提交数量，输出范围、页数、耗时、按开始年份统计和实际日期覆盖；加入零数据年份、日期异常、无对阵、跳过/保留记录清单及拟发布/下架数，不把准备数描述为新增数。
- [x] 4.2 实现并验证 `completed`/`needs_review`/`failed` 与退出码 0/2/1，仅显式补采采用新增退出语义；覆盖预览、零候选、缺失年份、回滚、提交结果未知、提交后缓存失败和跨年去重统计，失败时保留部分摘要。
- [x] 4.3 更新 `backend/cmd/wst_sync/README.md`，补充默认补采、指定范围、发布选项对已有赛讯的影响、日志保存、维护窗口等待既有任务结束并单进程运行、失败重跑和回退；给出构建后二进制的退出码用法，明确 go run 包装行为及重叠窗口仍会自动清理。

## 5. 验证与运行验收

- [x] 5.1 在 `backend/` 执行 `go test ./internal/logic/wstsync ./cmd/wst_sync`，确认相关参数、客户端、存储、图片和自动同步测试通过。
- [x] 5.2 在应用了现有迁移的隔离 MySQL 环境验证官方来源 ID 生成列唯一索引、软删除冲突、重复导入及事务回滚；准备测试 schema 遵循 testsupport 约定，不使用生产数据或仅靠 SQLite 结果。
- [x] 5.3 在选定环境执行 `--backfill --dry-run`，记录 2023 年至执行当天的各年数量、异常、请求页数、耗时和代表性内存占用，评估整段运行是否可行；真实接口不可用时记录阻塞，不用模拟数据替代结论。
- [x] 5.4 在有备份且所有实例 WST 自动任务暂停的目标环境完成正式导入；核对四类数据关联，抽查每个有数据年份的官方赛事/对阵/比分，重跑同范围验证幂等，保存验收结果并恢复自动任务。该项属于实际运行工作，不能仅因代码测试通过而勾选完成。
