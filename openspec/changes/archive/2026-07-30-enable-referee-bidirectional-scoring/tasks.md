## 1. 参与方映射与配置

- [x] 1.1 在 `app/utils` 新增最小纯函数，解析裁判固定选手1/选手2和选手相对我方/对手到 API actor/winner 的映射
- [x] 1.2 为局胜模式、九球追分和斯诺克生成角色相关的双方标签与操作反馈文案，避免页面处理器硬编码“对手/我方”
- [x] 1.3 新增纯逻辑测试，覆盖 player1、player2、referee 对参与方1和2的映射以及犯规方与得分方的语义差异

## 2. 裁判双向操作界面

- [x] 2.1 修改 `app/subPages/match/playing.vue` 的中式八球和美式九球操作区，为裁判展示选手1、选手2两套对应局胜入口并传入明确胜方
- [x] 2.2 修改九球追分操作区，为裁判提供双方普胜/小金/大金及双方犯规入口，同时保留选手现有单边操作区
- [x] 2.3 修改斯诺克操作区，增加独立的当前得分方和犯规方选择，并让进球、犯规请求使用所选参与方
- [x] 2.4 调整记分处理器、面板说明和成功 Toast，使裁判反馈准确指出实际得分方、胜方和犯规方，且继续复用现有 action id、revision 与同步响应流程
- [x] 2.5 在 `app/subPages/match/playing.scss` 补充双方操作区和斯诺克选择器所需的跨端样式，保持小屏可用并遵守 UniApp/WXSS 限制

## 3. 契约与后端回归

- [x] 3.1 将 `backend/chasing_points.api` 中记分 actor、犯规 actor 和局胜 winner 注释澄清为固定“1=选手1，2=选手2”语义
- [x] 3.2 修改 `.api` 后立即运行项目规定的 goctl 生成命令并检查生成差异，不手改生成文件
- [x] 3.3 补充或扩展后端对局测试，验证绑定裁判提交 actor/winner 1 和 2 时分别更新正确参与方

## 4. 验证

- [x] 4.1 扩展页面结构测试，确认四种球类的裁判模式均有双方入口，且斯诺克进球和犯规不再固定为 actor 2
- [x] 4.2 在 `app/` 运行 `node --test tests/*.test.mjs`，确认裁判双向记分和现有选手/只读流程全部通过
- [x] 4.3 在 `backend/` 运行相关对局测试及 `go test ./...`，确认契约澄清和裁判双方写入无回归
