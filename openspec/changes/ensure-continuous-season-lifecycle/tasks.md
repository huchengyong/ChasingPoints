## 1. API 契约与生成代码

- [x] 1.1 先扩展后端 API 契约测试，断言当前赛季响应和荣誉墙响应包含加法式 `season_state`，且原有字段名称与类型保持兼容。
- [x] 1.2 修改 `backend/chasing_points.api`，为 `GetCurrentSeasonResp` 和 `GetHonorWallResp` 增加 `season_state`，取值语义为 `not_started/active/unavailable`。
- [x] 1.3 修改 `.api` 后立即运行 goctl 生成代码，检查生成 diff，并确认没有新增未实现的 todo logic 或错误 handler 引用。
- [x] 1.4 更新生成 payload 与 handler 相关测试，覆盖 `season_state` 的 JSON 输出和空 season 兼容行为。

## 2. 赛季配置与数据库约束

- [x] 2.1 先扩展 migration 测试，断言新迁移安全检测重复起点、为 `seasons.start_date` 增加唯一约束/必要索引，且 Down 不删除赛季或用户资产。
- [x] 2.2 新增 MySQL 兼容的 goose migration，在无重复起点时创建唯一索引，并为冲突环境提供明确失败信息而不是静默改写数据。
- [x] 2.3 在后端配置中增加赛季生命周期的启用开关、月初锚点、初始编号、自然月周期和时区，并同步 `etc/chasing_points-api.yaml` 与 `.env.example`。
- [x] 2.4 增加配置解析与校验测试，覆盖默认 `Asia/Shanghai`、默认一个自然月、非法日期、非月初锚点、非正周期和非法初始编号。

## 3. 统一赛季时间窗口

- [x] 3.1 先为共享赛季窗口 helper 增加测试，覆盖业务时区、结束日期全天、相邻窗口边界、跨月/跨年周期和边界时刻唯一归属。
- [x] 3.2 实现统一的赛季策略归一化、确定性 `S{编号}` 窗口生成及 `[startInclusive, endExclusive)` 计算 helper。
- [x] 3.3 扩展 `SeasonModel` 的按开始日期查询、按有效时间查询、按日期顺序列举及幂等创建方法，避免继续用 `status = 1` 作为当前赛季唯一判断。
- [x] 3.4 将赛季挑战、挑战归档、赛季结算、赛季报告和相关 model 查询统一改为半开窗口，消除直接使用 `end_date` 当天零点或 `<= end_date` 的路径。
- [x] 3.5 更新 `RankRebuildService` 的比赛归季、段位日志和赛季记录重建，使其复用同一窗口 helper 和比赛完成时间。
- [x] 3.6 增加回归测试，验证结束日夜间事件、边界零点事件、跨边界完成比赛及赛事 `occurred_at` 在实时进度、结算、报告和重建中的结果一致。

## 4. 连续赛季生命周期服务

- [x] 4.1 先增加生命周期服务测试，覆盖未启用、锚点前、空表首次启用、补齐多届、当前加下一届、重复执行和既有正确窗口复用。
- [x] 4.2 实现 `SeasonLifecycleService` 的状态解析和确定性排期计划，确保从锚点到当前窗口及至少一个未来窗口连续存在。
- [x] 4.3 实现排期正式写入和 status 收敛，保留已完成结算，并在重复起点、重叠或日期不一致时返回可审计冲突而不覆盖历史。
- [x] 4.4 增加多实例并发测试，验证唯一索引、事务和条件更新可收敛为单一窗口，且不会产生重复活动赛季。
- [x] 4.5 将生命周期配置与服务接入 `ServiceContext` 和服务启动链路，配置未启用时保持现有接口可启动且 Worker 不写赛季数据。

## 5. 换季结算与 Worker 追赶

- [x] 5.1 先扩展 Worker/结算测试，覆盖先补齐排期、正常无缝换季、延迟激活已有进度、跨越多届后按顺序追赶和中途失败重试。
- [x] 5.2 为赛季结算增加显式通知策略，使正常换季保留现有通知，而历史修复可以禁止未读通知、Push 和 WebSocket。
- [x] 5.3 调整 `SeasonRolloverWorker`，每轮先确保连续排期，再循环处理全部已结束且未完成结算的赛季，最终收敛到按有效时间解析的当前赛季。
- [x] 5.4 加强结算幂等测试，确认事务失败、多个 Worker、重复运行及已完成结算不会重复挑战快照、赛季记录、称号或通知。
- [x] 5.5 增加资产保护回归测试，确认换季不应用 `rank_reset_ratio`、不降低段位、不清空生涯成就、不撤销称号且不改变佩戴状态。

## 6. 空窗数据修复能力

- [x] 6.1 先定义修复汇总结构并增加 dry-run 测试，覆盖待创建窗口、冲突、待结算赛季、事件/用户覆盖和预计资产变化，确认 dry-run 零写入。
- [x] 6.2 实现可复用的赛季生命周期修复服务，正式模式幂等创建缺失窗口并以静默策略结算已经结束的窗口，不复制或改写原始事件。
- [x] 6.3 新增 `backend/cmd/season_lifecycle_repair` 命令入口，支持 `--dry-run` 与正式执行并输出配置、计划、冲突、实际新增和失败信息。
- [x] 6.4 增加修复回归测试，验证当前空窗事件即时进入挑战、已结束窗口生成快照/记录/荣誉、冲突停止写入和第二次执行无重复新增。
- [x] 6.5 为修复命令补充最小运行说明，明确锚点之前数据保持生涯归属、正式执行前必须核对 dry-run，以及修复不会发送历史换季提醒。

## 7. 后端赛季状态与荣誉墙降级

- [x] 7.1 先扩展当前赛季 logic 测试，覆盖 `not_started`、`active`、`unavailable`、status 延迟但时间窗口有效及旧空 season 字段兼容。
- [x] 7.2 更新当前赛季、默认赛季排行榜和默认个人赛季记录读取路径，使用生命周期服务按业务时间解析当前窗口。
- [x] 7.3 扩展荣誉墙测试并更新 `fillSelfSeasonData`，在 active 时返回挑战，在 not_started/unavailable 时保留生涯成就和历届荣誉且填充准确 `season_state`。
- [x] 7.4 将赛季排期冲突或子域查询异常记录为可诊断日志，确保荣誉墙不会仅因当前赛季不可用而整页失败。
- [x] 7.5 运行赛季、荣誉墙、排行榜、报告和 handler 定向测试，确认好友视图隐私及原有 API 字段行为没有回归。

## 8. 移动端赛季状态体验

- [x] 8.1 先扩展 `app/tests/honor-wall.test.mjs` 和赛季页面源码测试，覆盖 active、not_started、unavailable 三种状态及旧响应兜底。
- [x] 8.2 更新 `app/utils/honor-wall.js` 的当前赛季状态视图模型，使用 `season_state` 区分“赛季尚未开启”“进行中”和“赛季数据更新中”。
- [x] 8.3 更新 `app/subPages/achievement/index.vue` 的荣誉墙当前赛季 Tab，active 展示原挑战，其他状态展示准确说明且不再把空数据统一称为“赛季间歇中”。
- [x] 8.4 更新 `app/subPages/season/index.vue`，根据接口 `season_state` 展示对应空状态，并保持球种切换、排行榜和报告入口只在有有效赛季时工作。
- [x] 8.5 增加旧后端响应兼容测试，确认缺少 `season_state` 但存在 season 时仍视为 active，完全空响应时使用保守的“赛季尚未开启”文案。
- [x] 8.6 运行 `cd app && node --test tests/*.test.mjs`，修复本变更引入的移动端回归。

## 9. 上线验证与交付检查

- [x] 9.1 运行 `cd backend && go test ./migrations`，验证索引迁移、重复起点保护和安全回滚测试。
- [x] 9.2 运行赛季生命周期、Worker、结算、修复、荣誉墙、报告和重建相关后端定向测试。
- [x] 9.3 运行 `cd backend && go test ./...`，确认完整后端测试通过。
- [x] 9.4 在测试数据库使用目标锚点执行修复 dry-run、正式执行和第二次正式执行，核对窗口连续、当前/下一赛季存在且重复执行零异常新增。
- [x] 9.5 核对当前赛季公开接口和本人荣誉墙分别返回正确 `season_state`，并验证结束日、边界零点和 Worker 延迟场景的数据归属。
- [x] 9.6 运行移动端全量测试、`git diff --check` 和 `openspec validate ensure-continuous-season-lifecycle --type change --strict`，确认实现与 proposal、design、specs 一致。

## 10. 审查整改

- [x] 10.1 调整生命周期排期与状态收敛顺序：排期创建不得提前关闭或激活赛季；每届结算事务负责状态切换，全部成功后再收敛初始/遗留状态，并覆盖失败重试。
- [x] 10.2 统一赛季结算、历史成就重建与段位重建的有效排位筛选，兼容历史 `win_type IS NULL`，并确保段位重建的赛季记录只统计同一口径比赛。
- [x] 10.3 修正赛季报告按球种胜场查询，覆盖用户作为双方任一方、排除练习赛和软删除/无效对局，并复用赛季结算口径。
- [x] 10.4 让赛季称号 `granted_at` 固定为权威赛季结束边界，而不是延迟结算、历史修复或重建的执行时间。
- [x] 10.5 扩充修复命令审计输出，逐项呈现策略、计划窗口、创建/结算状态、实际资产统计、冲突和失败，并在部分执行失败时保留已完成汇总。
- [x] 10.6 修正移动端赛季结束日的半开窗口计算：当前赛季响应提供权威 `start_at/end_at_exclusive`，旧响应兼容业务日期；并仅在 active 赛季展示球种切换、排行榜和相关入口，补充纯逻辑测试。
