## 1. 数据模型与 API 契约

- [x] 1.1 新增兼容 MySQL 的迁移：通过 `information_schema.COLUMNS` 安全添加 `snooker_format`、`snooker_target_wins`，并将存量版本2正奇数总局数回填为等价 `race_to` 目标胜局
- [x] 1.2 更新 Gorm Match 模型、赛制常量和规范化 helper，保留 `best_of_frames` 作为 legacy 兼容字段
- [x] 1.3 更新 `backend/chasing_points.api`，补充创建/详情/快照/WebSocket 所需赛制字段和赛制更新接口，并在修改后立即运行 goctl 生成代码
- [x] 1.4 补充迁移与赛制规范化测试，覆盖 legacy、free、race_to、目标范围1～25和存量阈值不变

## 2. 后端斯诺克赛制实现

- [x] 2.1 更新斯诺克创建校验：新客户端默认创建自由局数、缺省内部开球方使用兼容值，旧版本1和旧版本2 `best_of_frames` 请求继续兼容
- [x] 2.2 实现赛制更新 logic/handler：事务锁、创建者权限、无动作/无局/无裁判/无结束确认校验、base revision 冲突处理和权威快照返回
- [x] 2.3 在当前对局、对局详情、公开详情、同步快照和 WebSocket 中统一输出 `snooker_format`、`snooker_target_wins`、`can_change_snooker_format`
- [x] 2.4 将版本2单局结束逻辑改为按 `race_to` 目标胜局判断；自由局数在49局前不得因胜局数自动完成
- [x] 2.5 更新开始下一局逻辑，阻止已达到抢N目标或自由局数已完成49局的对局继续创建下一局
- [x] 2.6 实现统一正常结束协调：练习赛直接结算，排位赛进入现有结束确认；自由局数允许局间主动结束和平局
- [x] 2.7 调整整场认输与裁判判负逻辑：抢N局补足目标胜局，自由局数记录胜负但不伪造目标胜局
- [x] 2.8 广播赛制变更和达到结束条件后的状态更新，确保双方 HTTP/WS revision 与快照一致
- [x] 2.9 增加后端测试，覆盖自由局数平局/领先结束、零局或局中拒绝、49局上限、抢1/10/25、提前正常结束拒绝、排位确认、并发赛制更新和旧对局兼容

## 3. 移动端默认球种偏好

- [x] 3.1 新增按用户 ID 构建存储键并校验球种值的 `game-type-preference` 纯函数及 node:test 覆盖
- [x] 3.2 扩展 `gameTypeModal`：接收默认球种、打开时预选和标识默认项、提供明确的“设为默认”控制并返回确认元数据
- [x] 3.3 更新主页、对局、排行榜和我的页四个发起入口，读取同一用户偏好、保存明确设置，并保持临时选择不覆盖默认值
- [x] 3.4 在现有设置页增加“对局偏好”区域，支持四种球种和“每次询问”，并复用偏好 helper
- [x] 3.5 增加 Modal 与入口测试，覆盖无默认值、默认预选、临时切换、更新默认、清除、切号隔离和扫码取消后偏好保留

## 4. 移动端斯诺克自由局数与抢N局

- [x] 4.1 更新移动端 match API 和开局 payload，新斯诺克对局提交 `free`/0并移除扫码前的总局数与开球方 action sheet
- [x] 4.2 在斯诺克记分页增加赛制展示和原地滚动选择器，支持自由局数及抢1～25局，并展示 `2N-1` 最大局数说明
- [x] 4.3 根据服务端 `can_change_snooker_format` 控制交互；首次记分后移除箭头和点击态，冲突时应用权威快照而不弹阻断 Modal
- [x] 4.4 接入赛制更新接口与 WebSocket/同步快照，使创建者修改后双方页面同步显示相同赛制和目标胜局
- [x] 4.5 自由局数在至少完成一局且处于局间时展示“结束整场对局”，允许平局；抢N局隐藏该正常提前结束入口
- [x] 4.6 更新斯诺克页面状态与纯函数测试，覆盖自由/抢N文案、滚轮边界、锁定状态、平局结束、49局上限、排位确认和非斯诺克页面不受影响

## 5. 验证与回归

- [x] 5.1 对所有修改过的 Go 文件运行 gofmt，并检查 goctl 生成后没有新增未接入的 `todo` logic 空壳
- [x] 5.2 在 `backend/` 运行 `go test ./...`，修复新旧斯诺克规则、迁移、排位确认和 WebSocket 回归
- [x] 5.3 在 `app/` 运行 `node --test tests/*.test.mjs`，验证默认球种、四个入口、Modal 和斯诺克赛制交互
- [x] 5.4 人工核对普通/排位、四种球种、旧斯诺克对局、自由局数平局和抢N自动结束的关键用户路径
