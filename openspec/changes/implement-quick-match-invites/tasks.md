## 1. 数据与权威状态

- [x] 1.1 为 `challenges`、`matches` 和用户好友限制增加 goose 迁移与 Gorm 字段：约球保存预约日期/小时、比赛选项、失效时刻、等待方、归并 ID、终态原因；`matches.challenge_id` 用唯一可空索引保存权威关联（旧 `challenges.match_id` 不再写入），补有界查询索引；开发/测试/本地仅清理相关旧邀约数据，验证 Up/Down 与模型一致。
- [x] 1.2 在 `ChallengeModel` 实现按参与者/主记录 ID 的别名解析、有效活动与收到数读取、历史分页和事务状态更新；覆盖未执行 worker 时的过期过滤、稳定排序与历史去重。
- [x] 1.3 在服务端实现并测试北京时间合法区间计算：相对日与实际日期一致、24点边界、跨午夜编辑拒绝、已完全过去拒绝、次晨07:00生效。

## 2. 接口契约与发送/接受

- [x] 2.1 更新 `backend/chasing_points.api` 中约球、用户隐私、Bootstrap 及比赛当前/详情/同步快照的来源字段契约，并**紧接着**运行 `goctl api go --api chasing_points.api --dir . --style go_zero --home ~/.goctl/default`；检查生成 handler/logic 引用，不留下 `todo` 空壳。
- [x] 2.2 落实 `send`：校验登录身份、有效用户、好友或有真实比赛记录的对手资格（含不在最近列表中的再约对象）、接收方最新好友限制、四球种合法赛制和公开范围；事务限制一条自己发出的有效未回应邀请，覆盖并发送与拦截不占额度的测试。
- [x] 2.3 落实 `accept`：同人同球种/类型互邀归并为一个主记录，不同时间以被接受邀请为准；同人不同类型原状返回；换人接受在确认后与关闭自己旧邀请一起提交，补双方同时接受、旧邀请抢先被接受与接受失败回滚测试。
- [x] 2.4 落实 `reject|cancel|abandon` 与按身份校验的终态操作；仅允许未开局时关闭，记录原因并测试放弃与开局竞态无虚假比赛。
- [x] 2.5 扩展过期 worker 仅条件更新已到期未开局约球（含已接受/等待），验证没有 worker 时读/写到点拒绝、已开局不失效及过期邀约立即不占资格。

## 3. 双方进入与比赛关联

- [x] 3.1 实现 `enter|leave`：事务内按真实等待态判断无人等待时的提前进入确认（未确认不写等待），只更新本人等待态；第二人进入时在双方用户锁/邀请锁下核实比赛冲突和保存的比赛选项，沿用现有信誉禁赛等开局限制，并在同一事务中只创建和关联一场比赛。
- [x] 3.2 修改现有 `/api/match/start` 仅接受原有效签名扫码凭证，不允许凭 `challenge_id` 绕过双方进入；扫码开别场时同事务清除本人旧等待但保留有效约球，覆盖本人/对方比赛冲突。
- [x] 3.3 补并发/重放测试：首人不建比赛、双人同时进入/连点、仅按关联 ID 恢复、对方刚退出时提前提示、退出等待与进入竞争、取消先后、跨截止时刻及失败回滚。

## 4. 比赛终态与权限

- [x] 4.1 依据 `matches.challenge_id` 仅对约球创建的比赛调整排位 `finish_confirmation_required`、参赛方 `can_finish` 与正式 `/finish` 授权；在当前对局/详情/WS 快照返回一致来源标识；保留原赛制合法结束、裁判记分权限、非约球双确认和无权限拒绝，测试排位/练习与有/无裁判。
- [x] 4.2 在共享比赛结算事务中按比赛关联 ID 同步结束对应约球，覆盖手动、自动（灵活赛制/斯诺克）、平局、重复结束、旧请求和对方离线；不触碰后来新约球。
- [x] 4.3 将当前事务外读取再 `Save` 的 `CancelMatchLogic` 改为锁比赛并重检状态/版本的事务更新，在取消/作废路径与账号注销清理中核对关联约球终态和占用释放；测试取消与结束竞态、不生成虚假赛果及注销后另一方无残留阻塞。

## 5. 权威读取、设置与通知

- [x] 5.1 改造约球待回应列表、按 ID 详情与历史分页：按参与者鉴权，旧互邀 ID 解别名，过期实时显示「已失效」，历史含原因及通过 `matches.challenge_id` 查询的真实 `match_id`；高频查询做 query-count 测试和代表性 MySQL EXPLAIN。
- [x] 5.2 将当前约球、收到待回应数与服务端时间纳入 `/api/user/bootstrap`（约球区块独立 availability/partial error）；复用有界查询，测试读纯净、无约球与约球查询失败不污染当前比赛区块。
- [x] 5.3 用户隐私读写同时支持隐藏战绩与默认关闭的好友限制，按提交字段更新而非覆盖另一开关；补切号/读写失败测试，并核对既有账户数据导出/注销链路的新字段。
- [x] 5.4 通知/用户 WS 在事务提交后携带约球 ID 与状态失效提示；旧通知打开只读最新主记录，不泄露换人接受的新对象；漏消息可以 Bootstrap/详情恢复。

## 6. App 约球与双入口

- [x] 6.1 在 `app/api/challenge.js`/`user.js` 补接口门面，在 `activity` Store 接入 Bootstrap 约球摘要、收到数与服务端时间基准、账号/认证代次隔离、写操作/WS 标脏与前台回源；补局部失败及旧请求回写测试。
- [x] 6.2 新增约球发起表单及等待页并注册 `app/pages.json`：复用对手候选/偏好，选择球种、排位/练习、北京时间三日整点区间（默认下一可用整点）和可选附言（久远历史对手通过再约入口直达）；失败保留输入，提前且无人等待才提示，返回/锁屏不退出等待。
- [x] 6.3 将 `app/subPages/social/challenges.vue` 改为收到/发出/约球记录与详情：互邀合并、类型冲突、换人一次确认、拒绝/取消/放弃/退出等待；加载失败可重试且超时回源确认。
- [x] 6.4 在 `app/pages/match/index.vue` 和 `app/pages/user/index.vue` 同步当前约球卡及活动优先级；保留即时扫码开局和对局页常驻「约球记录」/「收到的邀请」，等待和比赛跳同一权威 ID。
- [x] 6.5 设置页增加「仅允许好友约球」开关，读失败不假装关闭、保存失败回滚；通知跳详情不自动进入，历史真实比赛详情添加预填但不发送的「再约一场」并限制非参赛者；将 `opponentSelector.vue`、报表/H2H 等原 PK 发送入口改为统一发起页，移除 `pending_match_challenge` 旧扫码上下文并更新相关测试。
- [x] 6.6 对约球正式比赛在 `app/subPages/match/playing.vue` 接入触摸/鼠标/键盘可用的滑动结束：终点松手才发 `/finish`，菜单等其他入口不能绕过滑块且无二次确认，中断/点击/失败复位，成功展示双方真实赛果；非约球现有结束流程不变。

## 7. 验证

- [x] 7.1 后端运行 `go test ./...`，App 运行 `node --test tests/*.test.mjs`；补最小跨端交互测试覆盖服务端时钟基准下的跨午夜/跨年展示、手机时区/时钟错误、活动卡优先级、滑动操作与身份切换。
- [ ] 7.2 双账号（再加第三账号）走 PRD 第9节：正常、放弃、退出等待再进入、返回锁屏恢复、互邀并发/换人接受、好友限制、扫码抢占等待、到期临界点、单方结束与取消终态；确认无重复比赛/结算及无遗留阻塞。
- [x] 7.3 验证本变更 OpenSpec 严格校验、goctl 生成文件/逻辑引用、迁移与索引；仅在相关本地/测试数据清理确认后才实施数据清理，不修改 HTML 预览实现。
