## 1. API 契约与生成代码

- [x] 1.1 在 `backend/chasing_points.api` 的 `AchievementDef` 中增加 `game_type`、`reward_title_name`，在 `HonorWallSummary` 中增加通用与球种专精拆分字段，并保持既有字段不变。
- [x] 1.2 修改 `.api` 后立即运行 goctl 生成代码，检查生成 diff 且确认没有新增未实现的 todo logic 或错误 handler 引用。
- [x] 1.3 更新后端 API payload 相关测试，覆盖新增字段的 JSON 名称、类型和旧字段兼容性。

## 2. 成就目录迁移与称号兼容

- [x] 2.1 先扩展 `backend/migrations/migration_files_test.go`，断言新迁移包含 25 项定义、13 个新增 Key、现有 Key 更新、非空图标路径和安全回滚语句。
- [x] 2.2 新增幂等 goose migration，插入 13 项新定义并按设计稿更新现有 12 项的名称、描述、`game_type`、排序、图标及奖励称号。
- [x] 2.3 在同一迁移中更新成就来源的既有用户称号展示名和 `source_ref_name`，保留 title key、记录 ID、来源引用与 equipped 状态。
- [x] 2.4 为迁移编写不删除既有用户资产的 Down 策略：恢复旧定义元数据，并将新增定义停用而不是破坏已有引用。
- [x] 2.5 运行 migrations 测试并核对 15 项通用、10 项球种专属及 10 项称号奖励与规格完全一致。

## 3. 后端成就响应与荣誉墙摘要

- [x] 3.1 更新成就列表、已解锁列表、赛季报告和荣誉墙的 `AchievementDef` 构建逻辑，统一填充 `game_type` 与 `reward_title_name`。
- [x] 3.2 扩展荣誉墙摘要计算，保留全目录兼容统计并按请求球种返回通用与专精的已解锁数和总数。
- [x] 3.3 更新荣誉墙后端测试，覆盖本人完整目录、好友仅已解锁、四球种专精统计、无效球种归一化和旧摘要字段兼容。
- [x] 3.4 更新成就进度服务测试，验证 `game_type = 0` 跨球种累计、球种专属隔离，以及九球追分与美式九球共享指标但不串进度。
- [x] 3.5 增加赛季回归测试，确认换季结算不会降低或清空生涯成就、进度和称号，赛季挑战仍按既有规则独立刷新。

## 4. 历史成就重建能力

- [x] 4.1 为有效历史比赛、按时间排序的双方胜负、特殊战绩来源和赛事参赛结果补充最小必要的 model 查询方法及单元测试。
- [x] 4.2 新增 `CareerAchievementRebuildService` 的汇总结构和 dry-run 路径，确保 dry-run 只计算用户、事件、解锁、称号与跳过数量而不写数据。
- [x] 4.3 实现对局数与胜场事件的幂等历史补齐，并从有序事件计算 sum 阈值首次跨越时间。
- [x] 4.4 实现按用户和球种回放历史胜负的连胜重建，生成可幂等的 `max_win_streak` 事件并计算 3/5/10 连胜首次达成时间。
- [x] 4.5 实现特殊战绩重建：优先接受可信 actor，必要时从回合或动作唯一推导；无法确认归属的记录必须跳过并计入汇总。
- [x] 4.6 实现赛事报名、有效完赛和冠军事件的历史补齐，并使用赛事业务时间计算首次达成时间。
- [x] 4.7 增加重建专用的用户成就和称号 upsert，使其保留较高进度、较早解锁时间、既有来源、既有称号和 equipped 状态，并支持历史 `granted_at`。
- [x] 4.8 为重建服务增加重复执行、部分历史缺失、旧 actor 不可信、已有解锁优先和不触发实时通知的测试。
- [x] 4.9 新增可运行的后端命令入口，支持 `--dry-run` 与正式执行，并输出可审计汇总和失败信息。

## 5. 移动端生涯成就视图模型

- [x] 5.1 先扩展 `app/tests/achievement-page.test.mjs` 和 `app/tests/honor-wall.test.mjs`，覆盖通用/球种拆分、固定分类顺序、专精筛选、即将达成排序和完成状态。
- [x] 5.2 在 `app/utils/achievement-page.js` 中增加球种标签、通用里程碑分组和球种绝技筛选纯函数，并保持好友只读列表只使用已解锁数据。
- [x] 5.3 在 `app/utils/honor-wall.js` 中增加拆分摘要和“即将达成”视图模型：仅包含通用与当前球种，按完成百分比和稳定排序取前三项。
- [x] 5.4 为零进度后备目标、全部适用成就已完成、无效数据和四球种切换补齐纯函数测试。

## 6. 荣誉墙与详情页体验

- [x] 6.1 调整 `app/subPages/achievement/index.vue` 的生涯 Tab，按“摘要、即将达成、通用里程碑、球种绝技”顺序展示，并保留当前赛季、历届荣誉和称号选择器行为。
- [x] 6.2 将顶部摘要改为通用完成度、当前球种专精完成度、赛季荣誉和赛事荣誉；切换球种时只更新专精区域与相关待完成项。
- [x] 6.3 确保好友视图不展示锁定进度或即将达成，仅按球种筛选已解锁绝技。
- [x] 6.4 更新 `app/subPages/achievement/detail.vue`，展示精确描述、球种、可选奖励称号、进度和解锁时间，移除统一的“累计达成 N 次”文案。
- [x] 6.5 更新页面源码断言和交互纯函数测试，确认新版结构、球种切换、详情字段及现有标题管理入口均符合规格。

## 7. 成就图标资源

- [x] 7.1 在 `app/static/images/achievements/` 建立对局、胜场、连胜、赛事、斯诺克、中式八球和九球七个系列母题，并提供目录中 25 项定义所引用的阶段变体资源。
- [x] 7.2 核对 migration 中每个非空 icon 路径都能解析到实际静态文件，且九球追分与美式九球资源可按 Key 区分。
- [x] 7.3 为列表和详情增加图片加载失败兜底，验证资源异常不会影响名称、进度、锁定状态和导航。

## 8. 回归验证与交付检查

- [x] 8.1 运行 `cd backend && go test ./migrations`，验证目录迁移与兼容回滚测试通过。
- [x] 8.2 运行成就、比赛、赛事、赛季和历史重建相关后端定向测试，修复本变更引入的回归。
- [x] 8.3 运行 `cd backend && go test ./...`，确认完整后端测试通过。
- [x] 8.4 运行 `cd app && node --test tests/*.test.mjs`，确认全部移动端纯逻辑与源码测试通过。
- [x] 8.5 对历史重建命令执行测试环境 dry-run 和重复正式执行，核对第二次执行不会增加事件、解锁或称号。
- [x] 8.6 运行 `openspec validate redesign-career-achievements --type change --strict` 并核对所有实现行为、迁移数据和图标资源与 proposal、design、specs 一致。

## 9. Review 修复与上线安全

- [x] 9.1 补充 `win_type IS NULL` 历史回合测试并修复有效比赛、批量回合查询的 NULL 兼容，避免历史对局、胜场和连胜漏算。
- [x] 9.2 将历史重建快照 upsert 改为数据库层单调合并，并增加陈旧重建快照不得覆盖实时较高进度、解锁、奖励与来源的回归测试。
- [x] 9.3 为荣誉墙请求增加 latest-request-wins 控制及延迟响应测试，确保快速切换球种或历史赛季不会应用旧响应。
- [x] 9.4 将历史重建的缺失事件与用户快照改为固定批次写入，并减少重复事件容器，降低全量执行的数据库往返和内存占用。
- [x] 9.5 运行后端、移动端全量测试、`git diff --check` 与 OpenSpec 严格校验，确认 review 修复无回归。
