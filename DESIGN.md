# 追分公开用户界面设计规范（DESIGN.md）

> 本文件是「官网 `website/`」与「App／小程序客户端 `app/`」公开用户界面的统一视觉规范入口。
> 管理后台 `admin/` 不在此规范范围内；除非有独立变更扩展范围，否则后台沿用自身 Element Plus 风格。

## 1. 适用范围

**适用：**

- `app/`：同时输出 App、微信小程序等平台的 UniApp Vue3 客户端。
- `website/`：Nuxt 3 官网与公开页面。

**不适用：**

- `admin/` 管理后台。
- 后端接口、数据模型、业务流程、页面信息架构与交互流程。

本规范统一的是品牌语义与组件层级，不要求官网与移动端像素级一致，也不要求两端共享组件代码。

## 2. 品牌原则

1. **金主绿辅**：金色承担品牌识别、主操作、选中态与荣誉表达；绿色只承担「成功 / 在线 / 进行中 / 正向变化 / 微信登录」等明确语义。
2. **暖中性表面**：页面、卡片、输入框与文字使用暖米白 / 暖黑金色系，不使用冷蓝灰作为基础表面。
3. **品牌一致性优先**：用户从官网进入欢迎 / 登录，再进入主流程，应感知为同一个品牌。
4. **状态可读性优先**：品牌金不承担警告 / 错误语义，绿色不承担普通品牌 CTA。

### 使用禁区

- 不要把绿色用于普通品牌 CTA 或非状态性主操作。
- 不要把金色用于警告或错误语义。
- 不要在卡片内大面积并列高饱和金与高饱和绿。
- 不要把冷 Slate / 冷蓝黑色作为页面、卡片、输入框、弹层或主要文字的基础色（数据图表、媒体遮罩、明确的信息语义除外）。

## 3. 颜色 Token

### 3.1 语义 Token 总表

| 角色 | 亮色 | 客户端暗色 | 用途 |
|---|---|---|---|
| `brand-primary` | `#E0AE12` | `#E0AE12` | 功能主操作、选中态 |
| `brand-strong` | `#C69200` | `#F0C542` | 品牌文字、重点数字 |
| `brand-gradient-start` | `#D9A617` | `#E0AE12` | 营销 / 重点 CTA 渐变起点 |
| `brand-gradient-end` | `#BB8400` | `#A97500` | 营销 / 重点 CTA 渐变终点 |
| `brand-tint` | `rgba(224,174,18,0.14)` | `rgba(224,174,18,0.20)` | 轻强调背景 |
| `surface-page` | `#F7F4EC` | `#141109` | 页面底色 |
| `surface-card` | `#FFFFFF` | `#1E180D` | 卡片、弹层 |
| `surface-subtle` | `#FAF8F2` | `#241D10` | 次级区域、输入框 |
| `text-primary` | `#231C0B` | `#FFF7E1` | 主文字 |
| `text-secondary` | `#6E6242` | `#D7C89B` | 辅助文字 |
| `text-muted` | `#9A8C67` | `#9F926E` | 弱提示 |
| `border-default` | `#E9E2CF` | `#3A2E16` | 卡片、输入框边界 |
| `success` | `#18B05B` | `#22C55E` | 成功、在线、正向状态 |
| `warning` | `#F97316` | `#F97316` | 警告 |
| `danger` | `#EF4444` | `#EF4444` | 错误、破坏性操作 |
| `info` | `#3B82F6` | `#60A5FA` | 信息提示 |

主品牌按钮文字统一使用 `#FFFFFF`。

### 3.2 高价值 / 重点表面

身份卡片、会员、荣誉墙、赛季高光、战报等「高价值」信息可使用暖黑金表面，例如：

- 亮色：`linear-gradient(135deg, #3C2B08, #8F6507, #C69200)` 一类暖棕金渐变，文字使用白色。
- 客户端暗色：`#1E180D` / `#241D10` 表面叠加品牌金 `brand-tint` 高光，文字使用 `#FFF7E1`。

媒体遮罩、封面压暗、图表配色允许使用冷色，但不得把冷色扩展为页面 / 卡片 / 输入框 / 弹层的基础表面体系。

### 3.3 实现映射

- App：在 `app/App.vue` 的 `page` 与 `.dark-mode` 中提供 `--ui-*` CSS 变量；`app/theme.json` 负责原生主题替换；`app/utils/theme-application.js` 负责运行时导航栏与 TabBar；`app/uni.scss` 保留 UniApp 默认变量。
- 官网：在 `website/assets/styles/main.css` 的 `:root` 中提供同名 `--ui-*` 变量，组件通过变量消费视觉角色。

## 4. 亮暗主题

- **亮色**：暖米白页面 `#F7F4EC`、白卡片 `#FFFFFF`、暖黑主文字 `#231C0B`。
- **客户端暗色**：暖黑页面 `#141109`、暖黑卡片 `#1E180D`、暖白主文字 `#FFF7E1`。
- 官网首期仅亮色；若后续新增暗色，应复用同一套暗色语义。
- App / 小程序暗色必须通过共享主题机制（`usePageTheme`）驱动，禁止用系统媒体查询绕过用户手动选择。

## 5. 排版

### 5.1 字号层级（App / 小程序，rpx）

| 层级 | 字号 | 用途 |
|---|---|---|
| Display | 54rpx | 欢迎 / 登录主标题 |
| Page title | 42rpx | 页面主标题、Hero 标题 |
| Section title | 30–34rpx | 区块标题、卡片标题 |
| Body | 24–28rpx | 正文、列表内容 |
| Caption | 20–22rpx | 辅助说明、元信息、标签 |

### 5.2 字号层级（官网）

| 层级 | 字号 | 用途 |
|---|---|---|
| Hero | `clamp(2.8rem, 6vw, 4.8rem)` | 首页主标题 |
| Section | `clamp(2rem, 4vw, 3rem)` | 区块标题 |
| Body | `1rem` / `1.08rem` | 正文 |
| Caption | `0.92rem` | 辅助说明 |

### 5.3 字体

- 中文：`PingFang SC`、`Hiragino Sans GB`、`Microsoft YaHei`、无衬线系统字体。
- 数字与比分类突出项可使用更大字重（700–900）与紧凑字距。

## 6. 间距

### 6.1 App / 小程序（rpx）

| Token | 值 | 用途 |
|---|---|---|
| `space-1` | 8rpx | 紧凑间距 |
| `space-2` | 12rpx | 标签间距 |
| `space-3` | 16rpx | 元素间距 |
| `space-4` | 20rpx | 卡片内间距 |
| `space-5` | 24rpx | 卡片 padding |
| `space-6` | 32rpx | 区块间距 |
| `space-7` | 40rpx | 大区块间距 |
| `space-8` | 48rpx | 页面大留白 |

页面容器统一使用 `box-sizing: border-box`；最外层容器注意首屏 margin collapse 与顶部漏白。

### 6.2 官网（px）

| Token | 值 | 用途 |
|---|---|---|
| `space-1` | 8px | 紧凑间距 |
| `space-2` | 12px | 标签间距 |
| `space-3` | 16px | 元素间距 |
| `space-4` | 20px | 卡片内间距 |
| `space-5` | 24px | 卡片 padding |
| `space-6` | 32px | 区块 padding |
| `space-7` | 48px | 大区块 padding |
| `space-8` | 72px | Hero 大留白 |

官网内容宽度统一为 `min(1120px, calc(100vw - 32px))`，`margin: 0 auto`。

## 7. 圆角

| 层级 | App / 小程序 | 官网 | 用途 |
|---|---|---|---|
| `radius-sm` | 12rpx | 8px | 小标签、输入框内元素 |
| `radius-md` | 18rpx | 12px | 按钮、输入框 |
| `radius-lg` | 24rpx | 20px | 卡片 |
| `radius-xl` | 32rpx | 24–28px | 重点卡片、弹层 |
| `radius-pill` | 999rpx | 999px | 胶囊按钮、Chip、标签 |

同类组件在两端应选择同一圆角层级。

## 8. 阴影

- 普通卡片：轻阴影或不加阴影，优先用 `border-default` 区分层级。
- 主操作按钮：品牌金柔和阴影（如 App `0 8rpx 32rpx rgba(224,174,18,0.22)`，官网 `0 18px 34px rgba(187,132,0,0.24)`）。
- 高价值卡片 / 弹层：较深投影表达层级，暗色主题改用深色投影（如 `rgba(0,0,0,0.2)`）。
- 阴影只辅助层级，不替代边框与对比度。

## 9. 组件规范

### 9.1 按钮

- 普通主操作：实色 `brand-primary`，白字。
- 营销 / 重点 CTA（官网、欢迎页、高价值入口）：可用 `brand-gradient-start → brand-gradient-end` 渐变，白字。
- 次要操作：透明或 `surface-subtle` 背景 + `border-default` + `text-secondary` / `brand-strong` 文字。
- 破坏性操作：`danger`，不与普通主操作同形。
- App 自定义 `button`：
  - `margin: 0`（覆盖原生 margin）。
  - 隐藏 `button::after`。
  - `line-height` 与 `height` 一致，保证文字垂直居中。
  - 禁用态使用 `[disabled]` 属性选择器，禁止 `&:disabled` 伪类。
  - 主题色按钮文字使用 `#FFFFFF`。

### 9.2 卡片

- 普通卡片：`surface-card` + `border-default` + `radius-lg/xl`。
- 高价值卡片：暖黑金表面 + 白字，仅用于身份、会员、荣誉、赛季高光、战报等强调场景。
- 卡片内不要混用多种高饱和强调色。

### 9.3 表单

- 输入框：`surface-card` 或 `surface-subtle` + `border-default` + `radius-md`。
- 聚焦 / 选中：`brand-primary` 边框或轻高光。
- 错误：`danger` 边框 + 明确的错误文案，不能仅靠颜色表达。
- 禁用：降低不透明度并保留可读性，不能与激活态混淆。

### 9.4 Chip / 标签

- 胶囊 `radius-pill`，选中态 `brand-primary` 白字或 `brand-tint` + `brand-strong` 文字。
- 状态标签使用语义色（success / warning / danger / info），不与品牌金混用。

### 9.5 导航与 TabBar

- App / 小程序选中态 `brand-primary`，未选中 `text-muted` / 平台未选中色。
- 导航栏、TabBar 颜色由主题机制统一驱动，页面不得硬编码绕过。
- 官网 Header 使用毛玻璃 + `border-default`，当前导航项与 CTA 保持清晰对比。

### 9.6 弹层 / 底部抽屉

- 弹层：`surface-card` + `radius-xl` + 深色遮罩。
- 底部抽屉覆盖 TabBar 区域时，必须处理 `env(safe-area-inset-bottom)`，或在打开时 `uni.hideTabBar`、关闭 / 卸载时 `uni.showTabBar`，避免操作与 TabBar 重叠。

### 9.7 空态 / 加载态 / 骨架屏

- 统一文字层级：标题（section title）→ 说明（text-secondary）→ 主操作（brand-primary）。
- 加载态与骨架屏使用与页面一致的表面色，不得引入页面专属中性色。
- 空态图标与按钮风格与品牌一致。

## 10. 响应式与可访问性

- 官网在移动端与桌面端均可正常浏览，无横向溢出；导航、栅格、卡片与 CTA 组按断点重排。
- 官网交互元素提供可见 `:focus-visible` 状态。
- 状态信息不能仅靠颜色传达，必要时补充文字。
- 正文与背景保持足够对比度；禁用态保持可读。
- App / 小程序触控目标与安全区遵循平台规范，自定义导航栏与系统高度 / 返回行为保持一致。
- 动效短促、克制（150–400ms），用于反馈而非装饰。

## 11. 平台单位映射

| 语义 | App / 小程序 | 官网 |
|---|---|---|
| 尺寸单位 | rpx | px / rem |
| 间距 | 8rpx 基础刻度 | 4px 基础刻度 |
| 圆角 | 12/18/24/32rpx + 999rpx | 8/12/20/28px + 999px |
| 主题 | 亮 + 暗（共享主题机制） | 首期仅亮色 |
| 组件 | 原生 `view` / `text` / `button` / `image` / `scroll-view` | HTML + scoped CSS |

微信小程序 WXSS 不支持 `*` 通配选择器，会编译到 MP-WEIXIN 的 `.vue` / `.scss` 禁止使用 `*`、`*::before`、`*::after`；仅供其他端使用的规则通过 `#ifndef MP-WEIXIN` 排除。

## 12. 页面验收清单

新增或改动公开页面时，逐项确认：

1. 主操作使用品牌金，白字，禁用态清晰。
2. 绿色仅用于成功 / 在线 / 进行中 / 正向 / 微信登录语义。
3. 页面、卡片、输入框、弹层使用暖中性表面 Token。
4. 亮暗主题均由共享主题机制驱动，暗色使用暖黑金。
5. 圆角、间距、阴影与同层级组件一致。
6. 自定义 button 满足 `margin: 0`、隐藏 `::after`、`line-height` 与 `height` 一致、`[disabled]` 覆盖。
7. 底部弹层处理安全区与 TabBar。
8. 空态 / 加载态 / 骨架屏层级统一。
9. 官网移动端无横向溢出，交互元素有可见焦点态。
10. 不改变路由、接口、生命周期、校验或业务数据。

## 13. 视觉 QA 矩阵

| 场景 | App 亮色 | App 暗色 | 微信小程序 | 官网移动端 | 官网桌面端 |
|---|---|---|---|---|---|
| 欢迎 → 登录 → 首页链路 | ✅ | ✅ | ✅ | — | — |
| 四个主 Tab | ✅ | ✅ | ✅ | — | — |
| 列表 / 详情 / 表单 / 弹层 | ✅ | ✅ | ✅ | — | — |
| 原生按钮 / 导航栏 / 安全区 | ✅ | ✅ | ✅ | — | — |
| 首页 Hero / 下载 CTA | — | — | — | ✅ | ✅ |
| 下载页 / 联系页 / 协议页 | — | — | — | ✅ | ✅ |
| 键盘焦点 / 卡片重排 / 横向溢出 | — | — | — | ✅ | ✅ |

> 自动化契约测试只覆盖可客观判定的颜色、Token、主题入口与禁用值；「是否好看」仍需按本矩阵人工验收。
